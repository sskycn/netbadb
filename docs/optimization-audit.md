# Optimization audit: storage ownership, round A

Initial baseline: `387a5a2b37733b42eae01ebd61dd949db313d593`. The Heap slice landed at `cd7d8c6c8e22d9739c39caca949aa213705c1098`; the LSM slice at `c28149bac6c3e9f1ed392a2959a927f5e53a47db`; the staged Heap handoff at `8bef52207aa728396b14ddc999d75cf03ba9ed76`. None is a measured hot-path optimization.

| ID | Current state | Call chain, trigger, and impact | Tests and this round |
| --- | --- | --- | --- |
| F01 | Confirmed; stage A remains partial | Direct Heap and LSM ownership, staged Heap handoff, and ordinary mixed Core admission have separate evidence below. A3.1 admits proven CreateHeap journal histories; other mixed schema mutation shapes, broader metadata lifecycle, and raw file APIs remain open. | The direct-engine and A2 tests remain required. A3 adds mixed admission and metadata contention regressions; A3.1 adds targeted journal replay tests. Neither closes F01. |
| F02 | Not established | Executor and Core resource paths were not traced in this round. | No change. |
| F03 | Not established | Synchronous worker deadline/cancellation paths were not traced in this round. | No change. |
| F04 | Not established | TLS and frame admission paths were not traced in this round. | No change. |
| F05 | Not established | Coordinator allocation frequency was not measured in this round. | No change. |
| F06 | Not established | Query response encoding path was not measured in this round. | No change. |
| F07 | Not established | BufferPool's `find_frame` is linear, but its workload cost was not measured in this round. | No change. |
| F08 | Not established | Prepare and authorization errors were not traced in this round. | No change. |
| F09 | Not established | CI already runs workspace tests, MSRV, independent consumers, storage compatibility, and Go interoperation; fuzz scheduling was not audited in this round. | No change. |

Heap ownership is a nonblocking advisory `flock` on the durable Heap data-file inode, held by the shared buffer state through its `PageManager`. The inode follows a staged-file rename, unlike a WAL generation, and does not conflict with Core's structured `<heap>.owner` identity file. The guard survives a dropped `HeapStorage` while a transaction still owns the buffer. A successful clean `close` synchronizes the files, verifies that no transaction is active, and unlocks the inode even if a completed transaction object remains in scope. The path's parent is canonicalized and subsequent I/O uses that path; file symlinks and hard links are rejected. The guard is acquired before WAL or recovery writes. A rejected open performs no file mutation. `inspect_recovery` uses read-only page and WAL handles without claiming mutation authority; Core inspects an already-open source Heap during replacement. It does not truncate incomplete WAL tails or remove WAL generations. A later recovery-style open independently acquires the inode lock before any mutation.

LSM direct-engine ownership is a nonblocking advisory `flock` on the LSM root directory inode. `create` acquires it before creating Manifest or WAL; `open_with_prepared_resolutions` acquires it before reading Manifest or WAL, orphan cleanup, or prepared-transaction resolution. The shared transaction state retains the directory descriptor if the storage object is dropped. A clean `close` verifies quiescence, flushes and syncs, then unlocks, including when a completed transaction handle remains. A rejected open cannot mutate the files, and `inspect_recovery` remains read-only while another process owns the engine. Canonical directory aliases share the same inode. No disk or wire format or sidecar layout changed.

The LSM same-process duplicate-open regression failed against `cd7d8c6c8e22d9739c39caca949aa213705c1098`: the second `LsmStorage::open` succeeded while the first remained live. With the directory guard, all seven ownership tests pass; the final test version also passed 30 consecutive parallel runs. The tests distinguish retained active transactions, completed transaction handles after clean close, read-only inspection, rejected-open file identity, subprocess contention, abnormal process exit, panic, failed recovery open, and directory symlink aliases.

LSM-slice validation on the pinned Rust 1.97.1 toolchain: `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --offline --locked`, `cargo clippy --workspace --all-targets --offline --locked -- -D warnings`, and `cargo test --workspace --offline --locked` passed; the workspace suite reported 1,879 passed and zero failed. The first sandboxed workspace run stopped at two CLI tests because loopback bind returned `PermissionDenied`; the complete suite then passed with local loopback permitted. `cargo +1.85.0 check -p netbadb-lsm --all-targets --offline --locked`, `cargo test -p netbadb-lsm --features test-hooks --offline --locked` (61 passed), `scripts/check-capability-deps.py`, `scripts/check-independent-consumers.sh`, and `scripts/check-baseline-change-compatibility.sh` passed. Cargo used a temporary offline vendor configuration. No throughput claim is made for this ownership fix.

A2 validation on macOS/aarch64 with Rust 1.97.1: `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --offline --locked`, `cargo clippy --workspace --all-targets --offline --locked -- -D warnings`, and `cargo test --workspace --offline --locked` passed on the final Rust source. The workspace run reported 1,896 passed, three existing ignored tests, and zero failures. Targeted `schema_mutation_tests` reported 83 passed and one ignored; Heap `ownership_` tests reported 15 passed. Deterministic additions include `staged_handoff_blocks_second_process_before_and_after_promotion`, `partial_promotion_keeps_source_name_owned_while_companions_move`, `recovery_handoff_keeps_promoted_heap_owned_until_final_open`, `changed_heap_inode_after_catalog_validation_fails_before_wal_recovery`, `later_heap_lock_failure_does_not_recover_earlier_participant`, `target_created_after_promotion_preflight_is_never_overwritten`, and `ownership_rebind_uses_new_sidecar_paths_and_frees_source_name`. Rust 1.85 checked Heap, Storage, and Core all targets. Core handoff and Heap ownership filters passed with `test-hooks`; capability dependency tests/checker, combined and seven minimal independent consumers, and the baseline storage compatibility script passed. Cargo used `CARGO_HOME=/tmp/netbadb-cargo-home-20260925`, whose temporary config points to an offline vendored source tree; no local path was added to the repository. No performance claim is made. Linux and network filesystem ownership semantics were not executed in this round.

The A2 Heap transfer is explicit. `PageManager` opens and locks the data inode; `BufferPool` and transaction handles may extend that open file description. Ordinary clean `close` flushes, checks quiescence, and explicitly unlocks; `stop_for_promotion` instead flushes and stops the writer, duplicates the locked descriptor, and returns a non-cloneable `HeapOwnership`. The old object cannot write after this transfer. Core moves owner/WAL/status/alternate names while the source data name and lock remain, then moves data last with an atomic no-replace rename. The token checks the final pathname against the same open inode before it is consumed by `open_with_ownership`, which derives WAL/status/change-stream paths from the final name. The duplicate descriptor is closed by Drop and never calls `LOCK_UN`, so dropping the stopped object cannot release the handoff lock. If the transfer fails, no live writer remains; the token (if still in scope) retains ownership until it drops, after which normal recovery can retry. A private staged Heap cannot enable a change stream through Core; unexpected stream sidecars at either name fail promotion closed, and direct token rebinding also rejects path-bound stream sidecars.

For coordinator recovery, Core acquires all Heap data locks in path order before inspecting prepared transactions. Recovery decisions use that inspection while the locks remain held; each token is consumed by the actual Heap open. Catalog path override discovery and `validate_physical` are read-only observations. `recover_physical_with_ownership` rechecks every Heap descriptor under the acquired inode locks before any participant recovery. Startup mutation replay retains Heap tokens across its internal open/close passes and hands them to the final registry open. A direct read-only inspection is diagnostic only and does not claim a consistent snapshot against an active writer.

## A3 mixed recovery admission (`6763bb6`)

| Boundary | State | Evidence and remaining limit |
| --- | --- | --- |
| Direct Heap owner | Covered by `cd7d8c6` | Data inode lock precedes WAL recovery; direct tests include subprocess contention and lifetime. |
| Direct LSM owner | Covered by `c28149b` | Root directory lock precedes Manifest/WAL recovery; direct tests include aliases and subprocess contention. |
| Staged Heap/Core handoff | Covered by `8bef522` | Non-cloneable same-inode token remains held across rename and final open. |
| Ordinary Heap/LSM Core admission | Implemented in A3; final validation below | Core claims both engine owners in pathname order before `CoordinatorLog::open` or participant recovery. It inspects every participant's recovery input before any writer opens, including catalogs without a coordinator. LSM ownership is consumed by the final writer without a second lock. Locked identities are checked against the catalog's storage IDs, table IDs and fingerprints. Mixed schema mutation replay with unresolved or complex journal history remains excluded. |
| Database metadata lifecycle | Partial | New Core create/open paths claim stable `.core-owner` sidecars for the catalog and coordinator, and retain them for the runtime. The coordinator can replace its log inode during compaction, so locking its current data inode would be insufficient. The sidecars use nonblocking `flock`; their creation is separate from modifying recovery files. Clean close unlocks after storage close; retained coordinator handles preserve abnormal-drop ownership. The mutation journal's full mixed replay participant discovery and all alternate metadata entry points still need a complete audit. |
| Raw file interfaces | Open | Public low-level page/WAL and direct metadata file tools have not been brought under this ownership boundary. |

The A3 write-path audit for ordinary mixed catalog open is:

| Function | Potential change | Required owner | Before A3 |
| --- | --- | --- | --- |
| `schema_mutation::recover` | Coordinator completion, staged promotion, journal/catalog publication and cleanup | Catalog, coordinator, every affected participant | Ran before ordinary participant claims; mixed unresolved journal replay now fails before modification. |
| `CoordinatorLog::open` | Truncate incomplete tail, synchronize log and parent | Stable coordinator owner and all recovery participants | Ran after Heap claims but before LSM claim. |
| `LsmStorage::open_with_prepared_resolutions` | WAL truncation and terminal records, Manifest sync, orphan cleanup, change-stream recovery | LSM root owner and all recovery participants | Claimed its own root only when reached in the open loop. |
| `HeapStorage::open_with_ownership` | WAL/status recovery and synchronization | Heap data owner and all recovery participants | Heap owners were already claimed by A2. |
| `configure_managed_projection_catalog` and close/Drop | Catalog generation updates, flush/checkpoint or cleanup | Retained metadata and participant owners | Runtime follows recovery; ownership now survives ordinary installation and completed transaction handles. |

`LsmStorage::inspect_recovery`, catalog `validate_physical`, and `CoordinatorLog::inspect_recovery` are read-only; they do not confer a stable snapshot by themselves. Core validates the locked participants against the read-only coordinator decisions before `CoordinatorLog::open`, which can truncate and synchronize, and checks that the writable open read the same decisions and checkpoint. On an admission or identity failure, ordinary mixed open performs no WAL, Manifest, Coordinator or Catalog recovery; the independent lock carrier may have been created. Once recovery begins, durable Commit/Abort/Prepare decisions follow the existing protocol and are not rolled back to satisfy a byte-for-byte failure assertion. The LSM transfer capability is non-cloneable and bound to the locked root inode; it is rechecked before being consumed. Existing disk and wire encodings are unchanged. Old binaries that ignore these new metadata sidecars cannot safely open concurrently. The behavior is currently verified only on macOS local storage; Linux local filesystem runtime and NFS/SMB behavior are not established by these tests.

Before skipping replay of a settled mixed create history, Core scans the Coordinator read-only under its stable owner and requires each committed reservation's Complete record or compacted checkpoint coverage. A pending schema mutation history fails closed before modifying recovery files. Terminal records can still carry cleanup work outside this ordinary-open slice; full mixed journal replay and cleanup admission remain stage A work. Metadata data-path symlinks and hard links are rejected so two pathnames cannot silently acquire different sidecar locks for one existing file.

The A3 admission regression was also run from an independent archive of `8bef522`, with the new integration test copied into that baseline. `mixed_admission_rejects_busy_participant_before_recovery` failed there because the Heap WAL's injected incomplete byte was already truncated while the LSM participant was still held by another process. It passes with A3. The positive mixed transaction crash matrix remains the existing `subprocess_heap_lsm_atomic_commit_crash_matrix_is_all_or_nothing`; the new admission tests distinguish rejection before recovery from legal recovery after admission. The Ubuntu `rust` CI job now runs the A3 contention, handoff, replacement and shared-metadata tests explicitly before its workspace suite. A native Linux result is pending; this macOS host has no Linux target/runtime, Docker, Podman or QEMU. Network filesystems require separate lock and rename validation. Process-crash tests do not establish complete power-loss durability.

On 2026-09-25, the final macOS tree passed `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, and `cargo test --workspace --locked --quiet` with an external offline Cargo vendor configuration. The workspace run included 696 passing Core unit tests (3 ignored), 394 Heap unit tests, 61 LSM unit tests and all 8 mixed LSM integration tests. The affected-crate Rust 1.85 check, Core mixed test-hooks filter, capability-dependency checks, independent/minimal consumer checks, and `scripts/check-baseline-change-compatibility.sh` also passed. The historical compatibility script checks sequential old/current reads and writes; it does not establish concurrent safety with older binaries. Linux runtime results remain pending despite the new Ubuntu CI entry.

The remaining stage A boundary is **database-wide ownership**, especially full mixed schema mutation replay, metadata lifecycle coverage across every entry point, and public low-level `PageManager`/`WalManager` raw file tools. After a direct Heap rename has completed, its former path can be reused safely as a different storage; a Core private staging pathname remains reserved for mutation replay and is not a general reusable user path. Direct-engine guarantees apply to `HeapStorage` and `LsmStorage` entry points; advisory locks protect against cooperating engines, not arbitrary file edits by a process with write permission. Disk-format compatibility does not establish safe concurrent access with older binaries that lack these ownership rules. The next phase after stage A is **stage B: network entry deadlines and payload admission**. Heap creation creates and locks its data inode before creating the WAL.

## A3.1 mixed schema journal follow-up (partial)

This round began at clean `6763bb6d702d511e726493379d7345acda8cd89b`; A3 was already committed, so there were no modified, staged or untracked files to preserve. The complete tracked source archive is `/private/tmp/netbadb-a31-baseline-6763bb6/source.tar` (SHA-256 `5e0133c0a93af9f2f868616aec8fa4152d3c38c48ea2e70cdeef53429379d4c5`), with a 691-path per-file SHA-256/mode/size manifest (`sha256-manifest.tsv`, SHA-256 `07ee554c3122959ab09ce9a1504c60e698054c78e262eed9bbf2434acd205432`) in the same directory. This is a local snapshot, not a repository artifact.

| Existing legal state | `8bef522` recovery | A3 `6763bb6` | Current A3.1 increment |
| --- | --- | --- | --- |
| LSM catalog plus one committed `CREATE TABLE` Heap composition, complete CORD winner and finished private cleanup | Reopens | Rejects the composition journal | Reopens after read-only completion, final owner, and cleanup-absence proof; ordinary mixed admission still claims all current Heap/LSM participants before recovery writes. |
| Same single-CreateHeap composition before a durable Commit decision | Rolls back private work | Rejects before recovery writes | Claims current LSM/Heap and the staged Heap, validates the exact private namespace, then rolls back through the existing journal protocol. |
| The single-CreateHeap Commit is durable, with prepared participant or promotion incomplete | Finishes the decided winner | Rejects before recovery writes | Claims both staged and promoted paths, checks the journal/Coordinator/Heap identities and split WAL evidence, then completes the original decision. |
| That winner is resolved, with private cleanup pending | Completes cleanup | Rejects before recovery writes | Replays the owned cleanup; a fully cleaned winner takes the read-only terminal path. |
| Legacy `create_heap_table_in` reservation beside LSM, including a transaction with current Heap and LSM DML | Reopens | Rejects an unfinished reservation | Admits the current Heap/LSM and staged or promoted new Heap before replay; preserves the original Commit outcome. |
| Sequential SQL and legacy CreateHeap histories with one unfinished latest record | Reopens | Rejects the unfinished journal | Proves all earlier create records settled and retains every current engine owner while replaying the latest record. |
| Heap ALTER/rewrite or DROP beside LSM; DDL targeting an LSM as a Heap rewrite | `UnsupportedPlacement` in the isolated `8bef522` probe | Not a recovery regression | Remains unsupported. |
| Ordinary `CREATE INDEX` on a Heap beside LSM | Succeeds and reopens without a schema mutation journal in the isolated `8bef522` probe | Reopens | Reopens on the ordinary mixed A3 path; no new journal admission needed for that operation. |
| Other index/backfill or mixed composition forms without an isolated legal fixture | Unverified | Conservatively rejected if they leave a non-create journal | No broadened admission without a proven resource plan. |
| Damaged journal, conflicting identity, or unexplained private resource | Fails under existing protocol checks | Rejected | Rejected; no ownership bypass is added. |

The clean completion row and two sequential SQL creates have direct three-state executable comparisons: each succeeds and reopens on an isolated `8bef522` archive, succeeds but fails to reopen at A3 `6763bb6`, and reopens with the current source. A current-source SQL child crash matrix exercises reservation, first staged file, materialized target, durable Commit, partial promotion, catalog publication, Coordinator completion, and winner resolution. A legacy reservation crash matrix exercises its pre- and post-decision windows, including partial promotion; a mixed current Heap/LSM DML transaction checks all decided participants. Each recovery case reopens twice and checks the original Commit decision. Historical crash fixtures for those incomplete windows have not been run against the old binary.

The terminal create proof holds current Heap inode ownership while checking the journal and exact private paths, then transfers those tokens into the normal mixed open. It requires a complete Coordinator decision or compacted checkpoint coverage, a create-only winner, a matching published Heap descriptor and stable `.owner` identity, all required final components, and absence of the exact private transaction directory. The pending single-CreateHeap path takes metadata owners first, then current Heap, untouched LSM, and any staged or promoted new Heap. Under those tokens it checks current and target identities, the exact private entries and owner record, component presence, prepared Coordinator participants, and unchanged journal/Coordinator bytes. A resolved winner with private cleanup pending also validates the digest of any remaining prepared catalog before deletion. The recovery engine retains both Heap and LSM tokens through its internal open/close passes and final registry installation; an LSM writer can return its existing directory token without a second flock. A partial Heap promotion can inspect its WAL at the promoted pathname while the data inode is still staged.

Busy LSM and staged Heap tests for both CreateHeap encodings compare catalog, journal and Coordinator bytes before retrying; a live LSM holder can still insert a row after the rejected open, and that row is visible after recovery. A barrier test pauses after full admission and verifies that independent opens of the staged Heap, LSM and catalog metadata are excluded until replay resumes. A second process crash immediately after replay finishes the partial promotion is followed by two successful reopens. Unknown private files and a mismatched staged owner are preserved and rejected without changing the three recovery files. The supported replay shape is a latest single CreateHeap (SQL table-object or legacy reservation) preceded only by proven settled create records. Other journal shapes still require their own old/staged/retired resource plans and remain rejected; F01 stays open until those legal-state boundaries and other stage A entry points are audited.

The prior intermittent LSM `WouldBlock` has no retained failing test name, subprocess trace or raw OS error, so its cause remains unknown. The current default-parallel 62-test LSM run and seven ownership tests passed. LSM `flock` contention now reports the root path, process ID, device/inode and original `raw_os_error`; the ownership regression checks that diagnostic. This distinguishes a future `flock` failure from another operation returning `WouldBlock`. No retry, sleep or lock weakening was added. Linux runtime and NFS/SMB behavior remain unverified here; the existing Ubuntu workflow entry has not been executed locally. Other metadata entry points and public raw page/WAL interfaces remain stage A work.

`libc` was already a workspace dependency. Heap and LSM use Unix `flock` because the standard library file-lock API is newer than Rust 1.85. Core uses macOS `renameatx_np(RENAME_EXCL)` or Linux `renameat2(RENAME_NOREPLACE)` to reject a target that appears after preflight; unsupported rename semantics fail closed. Non-Unix Heap and LSM creation/opening fail with `Unsupported` until a platform-specific ownership implementation exists. Local filesystem tests do not establish network filesystem lock semantics.
