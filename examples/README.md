# Examples

The first embedded example is exercised as an integration test in
`crates/netbadb-core/tests/embedded.rs`. A runnable CLI example will be added
when the command-line and server boundaries are defined.
